import os

SLEEP_TIME = 60  # seconds to sleep between server state checks
SERVER_TIMEOUT = 20  # seconds to wait for server to start or stop before exiting
OUTPUT_DIRECTORY = os.path.dirname("output")
